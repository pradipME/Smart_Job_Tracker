spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/jobtrack}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}

jwt.secret=${JWT_SECRET:am9idHJhY2tTZWNyZXRLZXlWZXJ5U3Ryb25nQmFzZTY0RW5jb2RlZEZvckpXVA==}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://jobtrack.netlify.app}

file.upload-dir=${FILE_UPLOAD_DIR:./uploads}

openai.api.key=${OPENAI_API_KEY:}
openai.api.url=${OPENAI_API_URL:https://api.openai.com/v1/chat/completions}
openai.api.model=${OPENAI_API_MODEL:gpt-4o-mini}

spring.cache.type=simple

springdoc.swagger-ui.enabled=${SWAGGER_ENABLED:true}
springdoc.api-docs.enabled=${SWAGGER_ENABLED:true}
